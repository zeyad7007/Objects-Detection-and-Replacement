# Install PyTorch SEPARATELY first (see README):
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
#
# Then:  pip install -r requirements.txt

# Detection + open-vocabulary
ultralytics>=8.3.0          # YOLOv8 / YOLO-World

# Segment Anything 2 (Meta)
# Installed from GitHub — see README. Listed here as a comment for reference:
#   pip install git+https://github.com/facebookresearch/sam2.git

# Inpainting
diffusers>=0.30.0
transformers>=4.44.0
accelerate>=0.33.0
safetensors>=0.4.5

# Image utilities
opencv-python>=4.10.0
pillow>=10.4.0
numpy>=1.26.0
matplotlib>=3.9.0

# Optional but recommended
huggingface_hub>=0.24.0     # for downloading SAM / SD weights
tqdm>=4.66.0
